/*
条件句の設定
*/

-- = : 一致
-- <>, != : 非一致
-- >, >=, <, <= : 数値の比較
-- A and B : A かつ B
-- A or B : A または B
-- () : 条件をくくる
-- like : %で部分一致検索
-- in : いずれかの値に一致
-- not in : いずれの値にも一致しない
-- between A and B: A から B の値 
-- is not null : null以外に一致
-- is null : nullに一致

-- 以上